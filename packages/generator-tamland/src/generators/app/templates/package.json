{
  "license": "UNLICENSED",
  "name": "generator-tamland-template",
  "description": "<%= project %> base",
  "scripts": {
    "build": "tamland build",
    "clean": "tamland clean",
    "deploy": "tamland deploy",
    "lint": "tamland lint",
    "local": "tamland local",
    "optimize": "tamland optimize",
    "setup": "tamland setup",
    "start": "npm run build && cd src/web && GCLOUD_PROJECT=tamland-example NODE_ENV=production node dist/server/index.js",
    "test": "tamland test"
  },
  "version": "1.0.0",
  "devDependencies": {
    "@tamland/cli": "^0.0.81"
  }
}
