

import "jasmine";


import { build } from ".";


describe("build", () => {

    it("{ build } is defined", () => {

        if(build){

            expect(build).toBeDefined();

        }

    });

});
