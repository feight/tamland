

const { h } = require("preact");

/** @jsx h */


module.exports = function render(props, content){

    return (
        <sidebar
            className="drawer"
            id={ props.id }
            open={ props.open }
            side={ props.side }
        >
            <div className="wrap">
                <div className="content-wrap">
                    <div className="edge">
                        <a className="close">
                            <span>&times;</span>
                        </a>
                    </div>
                    <div className="content">
                        { content }
                        { props.children }
                    </div>
                </div>
            </div>
        </sidebar>
    );

};
